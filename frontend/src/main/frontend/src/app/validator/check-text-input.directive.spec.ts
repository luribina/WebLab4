import { CheckTextInputDirective } from './check-text-input.directive';

describe('CheckTextInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTextInputDirective();
    expect(directive).toBeTruthy();
  });
});
